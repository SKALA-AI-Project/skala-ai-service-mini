# 설계-구현 매핑

## 1:1 대응표

- 목표 기술 3종 분석: `workflows/report_workflow.py` 초기 상태의 `topics`
- 경쟁사 2종 비교: `workflows/report_workflow.py` 초기 상태의 `competitors`
- 최근 3개월 범위: `agents/web_search_agent.py`의 `build_date_range`
- Supervisor 순차 제어: `workflows/report_workflow.py`의 실행 순서
- 검색 및 편향 점검: `agents/web_search_agent.py`
- TRL 평가: `agents/trl_analysis_node.py`
- 7개 섹션 초안 생성: `agents/draft_generation_agent.py`
- Markdown/PDF 산출: `agents/formatting_node.py`
- design.md 1:1 검증: `agents/supervisor.py`의 `validate_design_mapping`

## 현재 한계

- 실제 외부 API 연동은 키가 주어졌을 때 확장 가능하도록 골격만 구현함
- PDF는 외부 라이브러리 없이 생성 가능한 최소 파일로 우선 구현함
- LLM 기반 품질 평가는 규칙 기반 점수로 대체함
